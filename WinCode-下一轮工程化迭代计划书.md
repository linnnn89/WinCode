# WinCode 后续测试计划

更新：2026-09-11（北京时间）。当前版本为 **0.15.0 / main `d51f3e1`**。版本和 CI 状态见 [README](README.md)，实现方式见[架构说明](WinCode-架构与数据流说明.md)，历史结果见[工作日志](docs/codex_worklog.md)。

固定工作区、保持已加载的 Host、请求数量限制、独立的设计时输出目录和共享缓存回归测试已完成。PR #37/#38 已合并，Node 22/24 和 CodeQL 检查通过。这些内容已从待办中移除，下面只保留尚未完成的测试和未定位的问题。

## 待完成的测试与问题排查

| 事项 | 已知情况 | 下一步 |
| --- | --- | --- |
| 实际客户端中的 Roslyn 操作 | 已确认本机 Codex 连接运行 0.15.0 / local-text，WinCode Skill 已安装；尚未在当前版本的实际客户端中测完 Roslyn 流程 | 使用已配置 Roslyn 的连接，依次测试符号搜索、引用查询、影响分析和重构建议；修改源码后，确认旧符号位置被拒绝，重新搜索可恢复查询。原计划中的两个实际应用、A/B 并发任务和 Codex 测试仍需完成；agy CLI 测试继续暂缓 |
| 两个 MCP 连接同时检查 UI | 现有测试覆盖一个连接对同一窗口的并发请求，尚未覆盖两个独立 Gateway | 用 WPF 测试程序分别测试两个连接检查不同窗口、同时检查同一窗口。核对 PID/HWND、控件内容、截图是否来自指定窗口，以及辅助进程是否正常退出 |
| UI 和托盘的间歇性失败 | 仍有未定位的隐藏/唤出、截图/提示窗问题，以及托盘 `--endpoint` 启动超时记录；后续测试通过还不足以说明原因已经查清 | 复现时保存错误码、原生日志和各步骤耗时，定位后重测原操作。此前结果和缺失的原始报告已在工作日志中说明 |
| SDK 突发请求警告 | 128 个请求突发发送时曾出现 11 个 drain 监听器的警告，结束后监听器数量回到 0；服务端请求限制无法消除客户端已经产生的发送积压 | 先检查常见的连续任务是否受影响。若再次出现，检查客户端发送背压及监听器的注册与释放，不仅通过提高监听器数量上限来隐藏警告 |

## 遇到相关需求时再测试

- **长期运行和大项目：**观察 Node、Code Host 及其子进程的内存、CPU、句柄、队列和启动次数，测量大附件哈希计算的开销及磁盘占用。现有短时测试还不能判断长期运行是否泄漏。
- **特殊 MSBuild 项目：**使用实际项目测试自定义 target 动态生成的项目引用、复杂 source generator、外部输入，以及多个进程同时修改不同源码文件的情况。现有测试已覆盖两个 TFM、显式项目引用修改和两个读取实例对源码修改的响应。
- **异常退出后的文件清理：**尚未测试写入中途强制退出、Gateway 与 Host 同时强制退出或断电后的遗留文件。当前缓存附件可能被后续清理，磁盘限额通过定期清理实现，跨文件读取也不是原子快照；是否调整这些行为，应由实际需求决定。
- **其他 Windows 配置：**跨权限或登录会话、不同 DPI、Explorer 重启后的表现仍需测试。托盘的八实例容量和连接恢复已验证，不重复列为开发任务。

## 当前功能范围

继续使用独立 Gateway、每连接固定项目，默认使用 local-text，按需配置 Roslyn。Roslyn 不自动释放，用户可手动释放；本地开发和测试以 Windows 11 x64 为准。

自动空闲释放、pause/resume、自启动、全局停止、审计日志自动轮转、跨实例清理缓存、共享 Host 或数据库、通用租约状态机、跨平台移植和 UI 写操作均暂不开发。

测试完成后，删除对应待办并将结果记入工作日志。GitHub Release 尚未发布，源码合并和 CI 通过不能替代发布或实际客户端测试。
