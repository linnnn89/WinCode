# WinCode 迭代路线图

更新：2026-09-10 晚间交接（北京时间）。工作区 I:/WinCode，分支 codex/runtime-baseline-and-cleanup，源码版本 0.15.0。本轮保存为草稿 PR；N4 已接入源码但正式构建与验收未完成，未合并或发布。明日恢复顺序见 [详细计划](WinCode-下一轮工程化迭代计划书.md#2026-09-11-恢复顺序)，历次结果见 [工作日志](docs/codex_worklog.md)。

## 已确认路线与完成情况

独立 Gateway、连接固定项目、健康 Host 保持热态、有界受理。多个 Agent 可通过同一连接处理同一项目；不同项目或软件建立各自连接。自动释放继续关闭，保留设置内手动释放。

N1、N2、N3 的本地实现已完成：异根打开在副作用前拒绝；同根保留热态并保留真实故障恢复；每实例业务请求上限 32、状态槽 4、参数预算 64 KiB，排队使用共享截止时间，取消后按实际收尾归还容量。既有交付、owner guard、托盘、附件重建、A9 清理身份和 CI 失败证据留存继续作为基线，不重复建设。

N4 接入前的稳定基线：核心 417 项中 416 通过、1 项可选集成条件跳过；真实 Roslyn 22、E4 17、桌面 35 项及后续工作流、手动释放 10 轮通过。三实例 SDK 11 场景通过，采用分阶段启动同根第二 Host；不能据此声称同根并发已修复，也不覆盖最后的 N4 源码。今晚新增产物归属及 Roslyn 契约测试 23/23 通过，正式全链路验收留待明日。

[Grok 真实验收](test-tmp/request-admission/grok-acceptance.json)已使用当前 0.15.0/Roslyn 完成 7 次 MCP 调用：拒绝 B 根后仍能用 A 原定位查到 1 项引用，业务占用最终归零。Codex 单列待验；本次隔离 CLI 设置与 MCP 审批要求冲突，不能归为用户截图配置问题。按用户要求先不动 agy CLI。

## 接下来的工作

| 顺序 | 尚未完成的目标 | 完成判据 |
| --- | --- | --- |
| N4 优先 | 完成已接入的私有输出正式验收 | 修正验证脚本的生产入口与 UUID 归属；构建/核心检查后用正式 Host 跑默认 A/B/A 同时冷启动、兼容性、输入反例和退出回收 |
| N4 后续 | 共享存储、源码变更与 UI 边界 | 两真实进程的并发写入/清理/退出无错误正文；编辑后旧定位失效，新搜索恢复；窗口隔离单独验证 |
| N5 | 实际客户端完整消费闭环 | Codex 最新 build/provider/schema 与业务查询；两种实际软件、模型 A/B 并发和编辑闭环按真实证据补验 |
| N5 | 资源、原生残余失败与交付 | 原生内存/CPU/句柄峰值与长期趋势，历史提示窗/托盘根因，Node 22 和当前提交远端 CI 分别核实 |

同根同时冷启动仍是默认验收路径；--serialize-same-root-startup 仅为隔离 N3 与 N4 的诊断选项。修订原型已补兼容性、输入判定及归属回收证据，随后接入正式源码；最后接入尚未构建发布，现有 dist/native 是较早构建。Gateway/Host 同时硬退出的孤儿产物、复杂项目图等边界继续保留。

## 必须保留的边界

- Windows 11 x64 是当前开发/测试基准；其他系统与依赖版本不保证同样效果，跨平台移植延后。
- 默认 local-text，Roslyn 继续要求显式配置和项目求值许可；hello/托盘观察不触发加载，未知磁盘统计不是零。
- Gateway 仅清理自建资源；手动释放在排队、执行或取消收尾时拒绝。请求容量、10 MiB SDK 帧、缓存预算、Node RSS 和原生内存分别评估。
- 本轮 SDK 客户端 drain 监听器警告仍存在；没有抬高阈值。短时通过不等于无泄漏，原生复测通过不等于历史根因已修复。
- GitHub 连接器本轮 HTTP 403；Node 22/当前增量远端 CI 未核实。实际全局客户端设置、已安装 Skill、依赖和外部发布均按有效授权处理。

## 延后

自动 idle 释放、pause/resume、自启动、全局停止策略、审计自动删除、跨实例一键清缓存、共享 Roslyn 服务/数据库、跨进程通用 Lease/FSM、跨平台移植和大型 UI 扩展。并发与消费闭环稳定后，再决定 UI → XAML/C# → Roslyn 的产品深化。

维护方式：已完成开发移出待办，历史结果追加至工作日志；未定位失败和未验证范围持续保留。
